<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.daan.dao.ApplicationsDao">
<!-- 	 <cache type="org.mybatis.caches.memcached.MemcachedCache" />           -->
     <select id="findApplications" parameterType="Map" resultType="com.daan.domain.Applications" >
         SELECT
			app.id,
			app.code_no as codeNo,
			app.`name`,
			app.display_order as displayOrder,
			app.memo,
			app.`status`,
			app.time_version as timeVersion
		FROM
			applications app
		WHERE
			app.`status` = #{status}
     </select>
</mapper>
